import nextConfig from "eslint-config-next";

export default [
  ...nextConfig,
  {
    // You can add your custom rules or overrides here
    rules: {
      // Example: "no-unused-vars": "warn"
    },
  },
];
